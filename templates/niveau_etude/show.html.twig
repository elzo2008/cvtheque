{% extends 'base.html.twig' %}

{% block title %}NiveauEtude{% endblock %}

{% block body %}
    <h1>NiveauEtude</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ niveau_etude.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ niveau_etude.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('niveau_etude_index') }}">back to list</a>

    <a href="{{ path('niveau_etude_edit', {'id': niveau_etude.id}) }}">edit</a>

    {{ include('niveau_etude/_delete_form.html.twig') }}
{% endblock %}
