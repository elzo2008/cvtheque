{% extends 'base.html.twig' %}

{% block title %}SituationProfessionnelle{% endblock %}

{% block body %}
    <h1>SituationProfessionnelle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ situation_professionnelle.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ situation_professionnelle.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('situation_professionnelle_index') }}">back to list</a>

    <a href="{{ path('situation_professionnelle_edit', {'id': situation_professionnelle.id}) }}">edit</a>

    {{ include('situation_professionnelle/_delete_form.html.twig') }}
{% endblock %}
